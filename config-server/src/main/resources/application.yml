server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/coldstone-run/msa-config-sample.git
          default-label: main
          clone-on-start: true
          force-pull: true